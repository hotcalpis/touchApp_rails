.list-group
  - posts.each do |post|
    = link_to(post, class: 'list-group-item border-left-0 border-right-0 pt-4') do
      %h4
        = display_user_avatar(post.user, 35, 35)
        = post.title
      %h5.block.overflow-hidden
        .float-right
          = image_tag('tag_icon.svg', size: '18x18') if post.tag_list.present?
          - post.tag_list.each do |tag|
            %span.badge.badge-info.mr-1
              = tag
      .block
        .float-left
          = post.user.name
        .float-right
          = l(post.created_at, format: :long2)
        .float-right.mr-3
          = image_tag('like_icon_green.svg', size: '20x20')
          = post.likes_count
          = image_tag('comment_icon.svg', size: '20x20')
          = post.comments_count 
  = paginate posts
